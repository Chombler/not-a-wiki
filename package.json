{
  "name": "realm-grinder-reference",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "validate": "node scripts/validate_astro.mjs",
    "test": "BASE_PATH=/not-a-wiki npm run build && npm run validate",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.0",
    "typescript": "^6.0.3"
  }
}
